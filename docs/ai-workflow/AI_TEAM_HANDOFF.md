# Передача основному чату Tariff: AI-TEAM-SAFE-001

Дата: 2026-09-05. Статус: подготовлен изолированный пилот; НЕ ВКЛЮЧЁН.

## Исходная точка

Repository: `ivan88810900-star/tnved_starter_kit_v2`.
Base branch: `feat/ntm-official-full-contours`.
Base commit: `4c543c1dc636e8203e0b7e75b607e788702af6cd`.
Integration branch: `feat/ai-team-safe-staging`.

Проверить текущие refs GitHub перед интеграцией. Эта запись не утверждает,
что локальные незапушенные изменения другого чата видны или синхронизированы.

## Что НЕ изменялось

Основная ветка, main, исходники приложения, БД, принятые флаги, текущий focus,
AGENTS.md, scheduler, источники и нормативное enforcement. Не выполнены merge,
deployment, подключение платных аккаунтов, добавление ключей, включение variables.
Наличие файлов workflow не подтверждает наличие настроенных Environments.

## Scope этой передачи

Только `tools/ai_team/`, два новых документа `AI_TEAM_*` и три
`.github/workflows/tariff-ai-*.yml`. Не переносить всю продуктовую feature branch
в main автоматически. PR направляется в исходную рабочую ветку как draft.

Никаких новых постоянных write-агентов поверх текущей разработки.
Codex — кандидат-патч по узкой задаче. Claude — advisory review ограниченного diff.
Gemini — advisory сравнение с предоставленными выдержками, НЕ live/legal аудит.
Финального production quality gate и автослияния в этом пилоте нет.

## Что должен проверить основной чат

Прочитать `AI_TEAM_SAFE_ROLLOUT.md`, затем сравнить diff с текущим рабочим HEAD.
Сохранить DM-0013, текущие нормативные/semantic работы и их уже принятые состояния.
Провести независимый review нового контроллера, YAML, supply-chain pins и модели
доступов. Обратить внимание: код из PR не исполняется рядом с API-ключами;
validator Codex копируется в root-owned `/opt` до старта агента; после агента
контроллер не запускает никакой код из изменяемого checkout.

Повторить offline unit tests; отдельно проверить CI и sandbox live smoke,
когда владелец сохранит ключи непосредственно в защищённые Environments.
Не просить присылать ключи в чат, issues или screenshots.

Адаптер существующих официальных source reports к evidence contract, запуск
продуктовых тестов без ключей и автоматическое создание PR — следующие отдельно
согласуемые этапы, не скрытая часть готового пилота.

## Команда для продолжения

«Прочитай docs/ai-workflow/AI_TEAM_HANDOFF.md из feat/ai-team-safe-staging.
Сверь с текущим Tariff HEAD. Проверь и интегрируй только AI-TEAM-SAFE-001,
не меняя текущие задачи, приложение, БД, источники и флаги. Автозапуск, merge
и production rollout без отдельного согласования не включать. Секреты — только
через защищённые GitHub Environments».
